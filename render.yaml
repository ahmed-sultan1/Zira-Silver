# Deploy on Render: https://render.com — connect this GitHub repo, Render reads this file.
services:
  - type: web
    name: zira-silver
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: 22
